<%= form_for [post, post.comments.new] do |f| %>

  <div class="form-group">
    <%= f.text_field :body, class: 'form-control', placeholder: "Enter the comment" %>
  </div>

  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>